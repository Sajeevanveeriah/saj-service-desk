import { boolean, date, integer, jsonb, pgTable, serial, text, timestamp, uniqueIndex, varchar } from "drizzle-orm/pg-core";

const id = () => serial("id").primaryKey();
const created = () => timestamp("created_at", { withTimezone: true }).defaultNow().notNull();
export const settings = pgTable("settings", { id: id(), legalName: text("legal_name").notNull(), abn: varchar("abn", { length: 14 }).notNull(), gstStatus: varchar("gst_status", { length: 20 }).default("unconfirmed").notNull(), gstEffectiveDate: date("gst_effective_date"), contactEmail: text("contact_email"), paypalUrl: text("paypal_url"), updatedAt: created() });
export const users = pgTable("users", { id: id(), email: text("email").notNull(), role: varchar("role", { length: 20 }).notNull(), customerId: integer("customer_id"), revokedAt: timestamp("revoked_at", { withTimezone: true }), createdAt: created() }, (t) => [uniqueIndex("users_email_idx").on(t.email)]);
export const customers = pgTable("customers", { id: id(), kind: varchar("kind", { length: 20 }).notNull(), name: text("name").notNull(), email: text("email"), phone: text("phone"), createdAt: created() });
export const serviceRequests = pgTable("service_requests", { id: id(), reference: varchar("reference", { length: 30 }).notNull(), customerId: integer("customer_id"), category: text("category").notNull(), description: text("description").notNull(), preference: varchar("preference", { length: 20 }).notNull(), urgency: varchar("urgency", { length: 20 }).notNull(), status: varchar("status", { length: 30 }).default("new").notNull(), createdAt: created() }, (t) => [uniqueIndex("requests_reference_idx").on(t.reference)]);
export const jobs = pgTable("jobs", { id: id(), customerId: integer("customer_id").notNull(), requestId: integer("request_id"), status: varchar("status", { length: 30 }).default("new").notNull(), priority: varchar("priority", { length: 20 }).default("normal").notNull(), dueDate: date("due_date"), completionNotes: text("completion_notes"), createdAt: created(), updatedAt: created() });
export const quotes = pgTable("quotes", { id: id(), jobId: integer("job_id").notNull(), version: integer("version").notNull(), reference: varchar("reference", { length: 30 }).notNull(), status: varchar("status", { length: 20 }).default("draft").notNull(), validUntil: date("valid_until"), termsSnapshot: jsonb("terms_snapshot"), totals: jsonb("totals"), acceptedAt: timestamp("accepted_at", { withTimezone: true }), acceptedBy: integer("accepted_by"), createdAt: created() }, (t) => [uniqueIndex("quotes_job_version_idx").on(t.jobId, t.version)]);
export const quoteLines = pgTable("quote_lines", { id: id(), quoteId: integer("quote_id").notNull(), description: text("description").notNull(), quantity: integer("quantity").notNull(), unitCents: integer("unit_cents").notNull(), discountCents: integer("discount_cents").default(0).notNull(), gstRate: integer("gst_rate").default(10).notNull() });
export const invoices = pgTable("invoices", { id: id(), jobId: integer("job_id").notNull(), quoteId: integer("quote_id"), number: varchar("number", { length: 30 }).notNull(), status: varchar("status", { length: 20 }).default("draft").notNull(), netCents: integer("net_cents").notNull(), gstCents: integer("gst_cents").notNull(), grossCents: integer("gross_cents").notNull(), allocatedCents: integer("allocated_cents").default(0).notNull(), dueDate: date("due_date"), customerSnapshot: jsonb("customer_snapshot").notNull(), businessSnapshot: jsonb("business_snapshot").notNull(), termsSnapshot: jsonb("terms_snapshot").notNull(), issuedAt: timestamp("issued_at", { withTimezone: true }), createdAt: created() }, (t) => [uniqueIndex("invoices_number_idx").on(t.number)]);
export const payments = pgTable("payments", { id: id(), invoiceId: integer("invoice_id").notNull(), provider: varchar("provider", { length: 20 }).notNull(), externalReference: text("external_reference"), grossCents: integer("gross_cents").notNull(), feeCents: integer("fee_cents").default(0).notNull(), netCents: integer("net_cents").notNull(), status: varchar("status", { length: 20 }).default("pending").notNull(), paidAt: timestamp("paid_at", { withTimezone: true }), createdAt: created() }, (t) => [uniqueIndex("payments_provider_ref_idx").on(t.provider, t.externalReference)]);
export const attachments = pgTable("attachments", { id: id(), customerId: integer("customer_id"), storageKey: text("storage_key").notNull(), filename: text("filename").notNull(), mimeType: text("mime_type").notNull(), size: integer("size").notNull(), sha256: varchar("sha256", { length: 64 }).notNull(), createdAt: created() });
export const outbox = pgTable("outbox", { id: id(), dedupeKey: varchar("dedupe_key", { length: 200 }).notNull(), type: varchar("type", { length: 40 }).notNull(), payload: jsonb("payload").notNull(), status: varchar("status", { length: 20 }).default("pending").notNull(), attempts: integer("attempts").default(0).notNull(), availableAt: timestamp("available_at", { withTimezone: true }).defaultNow().notNull(), lastError: text("last_error"), createdAt: created() }, (t) => [uniqueIndex("outbox_dedupe_idx").on(t.dedupeKey)]);
export const auditEvents = pgTable("audit_events", { id: id(), actorId: integer("actor_id"), action: text("action").notNull(), entityType: text("entity_type").notNull(), entityId: integer("entity_id"), metadata: jsonb("metadata"), createdAt: created() });
export const sessions = pgTable("sessions", { id: id(), userId: integer("user_id").notNull(), tokenHash: varchar("token_hash", { length: 128 }).notNull(), expiresAt: timestamp("expires_at", { withTimezone: true }).notNull(), revokedAt: timestamp("revoked_at", { withTimezone: true }) }, (t) => [uniqueIndex("sessions_token_idx").on(t.tokenHash)]);
export const demoMarker = pgTable("demo_marker", { id: id(), isDemo: boolean("is_demo").default(true).notNull(), createdAt: created() });
export const serviceCategories = pgTable("service_categories", { id: id(), name: text("name").notNull(), active: boolean("active").default(true).notNull() });
export const rateCatalogue = pgTable("rate_catalogue", { id: id(), categoryId: integer("category_id"), description: text("description").notNull(), unitCents: integer("unit_cents").notNull(), active: boolean("active").default(true).notNull() });
export const contacts = pgTable("contacts", { id: id(), customerId: integer("customer_id").notNull(), name: text("name").notNull(), email: text("email"), phone: text("phone") });
export const serviceLocations = pgTable("service_locations", { id: id(), customerId: integer("customer_id").notNull(), label: text("label").notNull(), address: text("address").notNull() });
export const devices = pgTable("devices", { id: id(), customerId: integer("customer_id").notNull(), description: text("description").notNull(), serialNumber: text("serial_number") });
export const tickets = pgTable("tickets", { id: id(), customerId: integer("customer_id").notNull(), jobId: integer("job_id"), subject: text("subject").notNull(), status: varchar("status", { length: 20 }).default("open").notNull(), createdAt: created() });
export const messages = pgTable("messages", { id: id(), ticketId: integer("ticket_id").notNull(), authorId: integer("author_id").notNull(), body: text("body").notNull(), visibility: varchar("visibility", { length: 20 }).default("customer").notNull(), createdAt: created() });
export const jobTasks = pgTable("job_tasks", { id: id(), jobId: integer("job_id").notNull(), title: text("title").notNull(), completed: boolean("completed").default(false).notNull() });
export const appointments = pgTable("appointments", { id: id(), jobId: integer("job_id").notNull(), startsAt: timestamp("starts_at", { withTimezone: true }).notNull(), endsAt: timestamp("ends_at", { withTimezone: true }).notNull() });
export const timeEntries = pgTable("time_entries", { id: id(), jobId: integer("job_id").notNull(), minutes: integer("minutes").notNull(), unitCents: integer("unit_cents").notNull(), invoiceId: integer("invoice_id") });
export const materialEntries = pgTable("material_entries", { id: id(), jobId: integer("job_id").notNull(), description: text("description").notNull(), quantity: integer("quantity").notNull(), unitCents: integer("unit_cents").notNull(), invoiceId: integer("invoice_id") });
export const expenses = pgTable("expenses", { id: id(), jobId: integer("job_id"), description: text("description").notNull(), amountCents: integer("amount_cents").notNull(), attachmentId: integer("attachment_id") });
export const invoiceLines = pgTable("invoice_lines", { id: id(), invoiceId: integer("invoice_id").notNull(), description: text("description").notNull(), quantity: integer("quantity").notNull(), unitCents: integer("unit_cents").notNull(), gstRate: integer("gst_rate").notNull() });
export const documentSequences = pgTable("document_sequences", { id: id(), documentType: varchar("document_type", { length: 20 }).notNull(), nextValue: integer("next_value").default(1).notNull() }, (t) => [uniqueIndex("document_sequences_type_idx").on(t.documentType)]);
export const creditNotes = pgTable("credit_notes", { id: id(), invoiceId: integer("invoice_id").notNull(), number: varchar("number", { length: 30 }).notNull(), amountCents: integer("amount_cents").notNull(), reason: text("reason").notNull(), createdAt: created() }, (t) => [uniqueIndex("credit_notes_number_idx").on(t.number)]);
export const receipts = pgTable("receipts", { id: id(), paymentId: integer("payment_id").notNull(), number: varchar("number", { length: 30 }).notNull(), issuedAt: timestamp("issued_at", { withTimezone: true }).defaultNow().notNull() }, (t) => [uniqueIndex("receipts_number_idx").on(t.number)]);
export const webhookEvents = pgTable("webhook_events", { id: id(), provider: varchar("provider", { length: 20 }).notNull(), eventId: text("event_id").notNull(), payload: jsonb("payload").notNull(), occurredAt: timestamp("occurred_at", { withTimezone: true }), processedAt: timestamp("processed_at", { withTimezone: true }) }, (t) => [uniqueIndex("webhook_events_unique_idx").on(t.provider, t.eventId)]);
